packages: .
-- tests: True

packages: ./vendor/cabal/Cabal

-- avoid `setup`-components picking up the local unreleased Cabal-2.0
constraints: setup.Cabal < 2

-- package hackage-server
--   flags: +build-hackage-import
